ROOTFS_PART=/dev/mmcblk0p7
RESIZE=resize2fs
if [ ! -b $ROOTFS_PART ];then
	echo "not available"
else
	echo "******* resize2fs the mmcblk0p7 ****************"
	if [ ! -a $RESIZE ];then
		echo "not available"
	else
		$RESIZE $ROOTFS_PART
	fi
fi
