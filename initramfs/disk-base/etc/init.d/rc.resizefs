ROOTFS_PART=/dev/mmcblk0p7
RESIZE=/sbin/resize2fs
if [ ! -b $ROOTFS_PART ];then
	echo "$ROOTFS_PART is not available!"
else
	echo "******* resize2fs the mmcblk0p7 ****************"
	if [ ! -x $RESIZE ];then
		echo "$RESIZE is not available!"
	else
		$RESIZE $ROOTFS_PART
	fi
fi
